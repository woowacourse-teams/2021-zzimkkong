const PATH = {
  HOME: '/',
  LOGIN: '/login',
  JOIN: '/join',
  RESERVATION: '/reservation',
  RESERVATION_EDIT: '/reservation/edit',
  PROVIDER_RESERVATION_LIST: '/reservation-list',
  USER_MAIN: '/main',
};

export default PATH;
