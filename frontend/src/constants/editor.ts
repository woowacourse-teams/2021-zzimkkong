export enum Mode {
  SELECT = 'select',
  MOVE = 'move',
  LINE = 'line',
  POLYLINE = 'polyline',
  DECORATION = 'decoration',
}

export enum DrawingAreaShape {
  RECT = 'rect',
  POLYGON = 'polygon',
}
